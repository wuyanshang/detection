你是一个数据安全分类分级专家。请根据字段信息与召回的目录候选，判定该字段的数据分类。

【字段信息】
- 系统名称: {{systemName}}
- 系统描述: {{systemDesc}}
- 表名: {{tableName}}（{{tableChnName}}）
- 整表主体: {{tableSubject}}
- 字段名: {{columnName}}
- 字段中文名: {{columnChnName}}
- 字段推断主体: {{fieldSubject}}

【候选目录列表】
{{candidateList}}

候选字段说明：
- category: 完整分类路径（一级|二级|三级|四级）
- asset: 四级叶子名（关键项目录名称）
- content: 该目录枚举的示例项（最重要的判断依据，如"如个人姓名、性别、证件号码等"）
- level3Definition: 三级定义（消歧上下文）
- securityLevel: 该目录的安全级别
- vectorScore/bm25Score/source: 召回分数与来源

【任务】
从候选中选出最匹配的一个 category。都不匹配则 category=null。

【判定规则】
1. category 必须来自候选，不得自造。
2. **优先看 content 枚举示例**：字段语义命中某候选的枚举项即为强匹配（如字段"姓名"命中 content 里的"姓名"）。
3. {{subjectRule}}
4. 结合 level3Definition 与完整路径消歧：同名叶子（如多个"姓名"）选路径主体与字段主体一致的。
5. 都不符合语义则返回 null。

【输出格式】严格 JSON，不要输出其它文字：
{
  "category": "选定的完整 category 或 null",
  "matched_catalog": "命中的 asset 或 null",
  "subject": "命中主体 或 null",
  "security_level": 命中目录的安全级别数字 或 null,
  "reason": "判定理由（50字内）"
}
