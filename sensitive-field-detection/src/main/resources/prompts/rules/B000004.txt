## B000004 - 字段中文名英文缩写不明确
**仅针对 field_cn 本身含的英文缩写**（table_en/field_en 中的缩写不触发）。
**判定**：field_cn 中的缩写，即使结合所有信息仍有多种合理展开、且不同展开指向不同业务含义 → 报。
- 报：field_cn=`tp`（Third Party? Transaction Price?）
- 不报：缩写在保险/金融领域有公认含义，或已被上下文收窄到一种。
**兜底**：缩写能在上下文中合理收窄到一种含义、或拿不准时，**默认不报**。
