## B000003 - 字段中文名是枚举取值、不承载字段含义
**判定**：field_cn 看起来是某个**取值/枚举值本身**，而不是对"字段存什么"的描述（回答了"值是什么"，没回答"这是什么字段"）。
- 报：`男`、`已婚`、`借方`、`正常`
- 不报：`性别`、`婚姻状态`、`借贷方向`（是字段名）；`险种类型`、`代码类型`（有清晰的分类维度，能理解）。
**兜底**：拿不准是取值还是字段名时，**默认不报**（视为字段名）。
